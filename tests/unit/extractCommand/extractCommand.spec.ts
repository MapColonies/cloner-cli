describe('extractCommand', () => {
  afterEach(() => {
    jest.resetAllMocks();
    jest.restoreAllMocks();
  });

  describe('handler', () => {
    it('add test', () => {
      expect(true).toBeTruthy();
    });
  });
});
